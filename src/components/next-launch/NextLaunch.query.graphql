query NextLaunchQuery {
  launchNext {
    id
    rocket { rocket { id name type } }
    launch_year
    launch_site { site_name_long }
    launch_success
    launch_date_utc
    details
    mission_name
  }
}
